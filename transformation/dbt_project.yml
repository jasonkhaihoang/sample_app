name: duckdb_local
version: "1.0"
profile: duckdb_local

model-paths:
  - models
seed-paths:
  - seeds
macro-paths:
  - macros

models:
  duckdb_local:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts

seeds:
  duckdb_local: {}
